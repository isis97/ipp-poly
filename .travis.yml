dist: trusty

sudo: required

language:
- cpp

compiler:
- gcc

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - cmake
    
script:
- gcc -v && g++ -v && cmake --version
- mkdir release
- cd release
- cmake ..
- make
- make test
- cd ..